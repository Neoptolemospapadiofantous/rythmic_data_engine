/*  ═══════════════════════════════════════════════════════════════════════════
    orb_strategy.cpp — implementation stub

    All strategy logic is inline in orb_strategy.hpp (single-TU model matches
    the existing codebase pattern where header-only classes dominate).
    This .cpp exists so CMake has a concrete source file for the static lib.
    ═══════════════════════════════════════════════════════════════════════════ */
#include "orb_strategy.hpp"
// No additional definitions — everything is inline in the header.
